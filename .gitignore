.DS_Store
.claude/settings.local.json

# Wrangler writes local secrets here in plaintext for `wrangler dev`.
# This must never be committed — the repo is public.
.dev.vars
.dev.vars.*
worker/.dev.vars
worker/.dev.vars.*

# Wrangler build/state
.wrangler/
worker/.wrangler/

# Anything explicitly marked local
*.local
*.local.js
*.local.json
